<UserControl x:Class="MessengerWPF.UserControls.ThreadControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" VerticalContentAlignment="Top">
    <Border BorderBrush="Black" BorderThickness="1" Margin="2" VerticalAlignment="Top" CornerRadius="6" Background="#FF00337E">
        <Grid VerticalAlignment="Top" Margin="2,5,2,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="ThreadUsers" Margin="10,0,0,0" TextWrapping="Wrap" Text="TextBlock" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="16"/>
            <TextBlock x:Name="LastMessage" Margin="0,3,0,5" Foreground="#FFD1D1D1" VerticalAlignment="Top" Grid.Row="1" TextTrimming="CharacterEllipsis" Padding="8,0"/>

        </Grid>
    </Border>
</UserControl>
