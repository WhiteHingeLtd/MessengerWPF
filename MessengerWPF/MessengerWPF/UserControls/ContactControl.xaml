<UserControl x:Class="MessengerWPF.UserControls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MessengerWPF.UserControls"
             mc:Ignorable="d" VerticalContentAlignment="Top">
    <Border BorderBrush="Black" BorderThickness="1" Margin="2" VerticalAlignment="Top" CornerRadius="6" Background="#FF00337E">
        <Grid Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="ThreadUsers" Margin="2.25,2" Foreground="White" FontSize="14" Content="" Background="{x:Null}" BorderThickness="0.5" BorderBrush="{x:Null}"/>
            <Button x:Name="AddToThreadButton" Grid.Column="1" BorderThickness="0.5" Margin="2,2,1,2" BorderBrush="{x:Null}" Background="{x:Null}" HorizontalAlignment="Right" HorizontalContentAlignment="Right">
                <Image Height="12.5" Width="12.25" Source="addicon.png" HorizontalAlignment="Right"/>

            </Button>


        </Grid>
    </Border>
</UserControl>

